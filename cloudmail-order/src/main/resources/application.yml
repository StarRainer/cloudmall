spring:
  application:
    name: cloudmail-order
  datasource: # 配置 MySQL 数据源
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123546}
    url: jdbc:mysql://${DB_HOST:localhost}:3306/cloudmail_oms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 12000
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml # 配置 mapper.xml 的路径
  configuration:
    database-id: auto # 表的主键为自增主键